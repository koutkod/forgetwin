# Optional canonical origin used for social-card metadata.
NEXT_PUBLIC_SITE_ORIGIN=http://localhost:3000

# Optional model selection. Each visitor supplies their own OpenAI API key in the app.
OPENAI_MODEL=gpt-5.6-sol
