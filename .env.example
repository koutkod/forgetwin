# Optional canonical origin used for social-card metadata.
NEXT_PUBLIC_SITE_ORIGIN=http://localhost:3000

# Optional. When absent, ForgeTwin uses the disclosed deterministic engineer.
# Keep this server-side; never prefix it with NEXT_PUBLIC_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
